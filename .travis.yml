sudo: false

before_script:
  - mkdir build
  - cd build
  - cmake ..

script:
  - make
  - make test

addons:
  apt:
    sources:
      - george-edison55-precise-backports
      - ubuntu-toolchain-r-test
    packages:
      - cmake
      - cmake-data
      - g++
      - libboost-all-dev

